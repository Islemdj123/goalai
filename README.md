# AI Football Predictor Dashboard ⚽📊

هذا المشروع يوفر واجهة رسومية متطورة لتحليل وتوقع مباريات كرة القدم باستخدام تعلم الآلة (Machine Learning).

## ✨ المميزات الجديدة
- **واجهة عصرية**: تصميم يعتمد على `Streamlit` مع نظام ألوان داكن وبطاقات (Cards) احترافية.
- **توقعات حية**: جلب تلقائي للمباريات القادمة وعرض التوقعات فوراً.
- **تحليل Winner & BTTS**: احتمالات الفوز وتوقع تسجيل كلا الفريقين.
- **نظام الثقة (Confidence System)**: تمييز المباريات القوية بشارة 🔥 HIGH_CONFIDENCE.

## 🚀 كيفية التشغيل

### 1. تثبيت المتطلبات
```bash
pip install -r requirements.txt
```

### 2. الإعداد
تأكد من وضع الـ `API_TOKEN` في ملف `config.py` وتدريب النماذج مسبقاً:
```bash
python src/fetch_data.py
python src/preprocess.py
python src/feature_engineering.py
python src/train_model.py
```

### 3. تشغيل الواجهة الرسومية
```bash
streamlit run dashboard.py
```

## 🧠 منطق العمل
1. **توقع الفائز**: يتم باستخدام `RandomForest` الذي يحلل معدل الأهداف لكل فريق.
2. **توقع BTTS**: نموذج مستقل يتنبأ باحتمال تسجيل كلا الطرفين بناءً على القوة الهجومية والدفاعية.
3. **الثقة العالية**: تظهر عندما يتجاوز احتمال الفوز 75% مع انخفاض احتمال التعادل.

## ⚠️ تنبيه هام
- التوقعات هي **احتمالات إحصائية** وليست حقائق مؤكدة.
- لا يقدم النظام أي ضمانات للربح.
- هذا العمل مخصص لأغراض البحث والتعلم فقط.
